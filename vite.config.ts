import { defineConfig } from 'vite'

// The site is served from https://kaili-kameoka.github.io/challenge-grid-landing-page-vanilla/,
// so built asset URLs need that repo name as their base.
export default defineConfig({
  base: '/challenge-grid-landing-page-vanilla/',
})
